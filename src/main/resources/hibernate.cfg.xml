<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">
<hibernate-configuration>
    <session-factory>
        <!-- H2 in-memory DB -->
        <property name="hibernate.connection.driver_class">org.h2.Driver</property>

        <!--
          mem:testdb = in-memory; DB_CLOSE_DELAY=-1 keeps it alive for the JVM lifetime
          MODE=Oracle makes H2 accept more Oracle-like SQL (useful if you plan Oracle later)
        -->
        <property name="hibernate.connection.url">
            jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=Oracle
        </property>

        <!-- H2 defaults: user 'sa' with empty password -->
        <property name="hibernate.connection.username">sa</property>
        <property name="hibernate.connection.password"></property>

        <!-- Use H2 dialect (even when MODE=Oracle) -->
        <property name="hibernate.dialect">org.hibernate.dialect.H2Dialect</property>

        <!-- Dev settings -->
        <property name="hibernate.hbm2ddl.auto">update</property>
        <property name="hibernate.show_sql">true</property>
        <property name="hibernate.format_sql">true</property>

        <!-- Your annotated entity classes -->
        <mapping class="org.example.hibernate.model.WeatherRecord"/>
    </session-factory>
</hibernate-configuration>
